import type { NextPage } from "next";

const Home: NextPage = () => {
    return (
        <div>
            <p className={"font-bold text-7xl"}>Hello, World!</p>
        </div>
    );
};

export default Home;
